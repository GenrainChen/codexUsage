[project]
name = "codex-usage-note"
version = "0.1.0"
description = "Local Windows sticky note for Codex token usage and list-price estimates"
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
start-directory = "tests"
