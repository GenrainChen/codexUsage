# 验证记录

2026-09-13，Windows / Python 3.13.13 / 项目 `.venv`。

- 完整 unittest：34 项，32 通过，2 项需显式启用的 GUI 布局测试默认跳过。
- 编译检查：`python -m compileall -q app.py usage_note` 通过。
- 真实 CLI 报告：扫描 176 个本地会话文件；计数、日期、模型和价格接口均成功。未知模型美元字段为 null。
- 首次扫描约 1.4–2.2 秒；没有新增内容的重复扫描约 0.07 秒、读取 0 字节。
- 真实窗口已检查日汇总、模型输入／缓存／输出、历史日期和价格编辑入口。启动器最初隐藏主窗，已修改为正常显示并重新启动验证。
- 布局回归包含 460×800、390×650 两尺寸的底部说明和长来源价格编辑保存按钮。实现代理曾执行独立 Tk 验证并通过；最终完整测试默认跳过 GUI，避免弹窗。
- 用户通过 Esc 停止 Computer Use 后，没有再操作用户便签窗口。当前已打开实例需要关闭后重新打开，加载最后的读取器和最小尺寸布局修正。

独立 code-review 检查了计价、启动器和读取器。复现并修复了复制快照差分覆盖、同长度原地文件重写、单次用量与累计用量混合导致重复计数的问题，相应回归测试已加入。

现存数据提示：两条分页继承累计基线，按可确认单次用量统计；一条缺少中间快照，按累计差分恢复并归于当前事件的时间和模型。原始日志不完整时，无法保证所有历史消耗都能恢复。

## 图标与标题栏更新

同日后续请求：添加橙色折角便签图标，包含 16–256px 多尺寸 ICO、32/128px PNG 和 SVG 源文件；更新桌面快捷方式图标及应用任务栏身份。主窗口和对话框通过 Windows DWM 使用内容区相同的背景、文字、边框颜色。

完整单元测试仍为 32 通过、2 GUI 默认跳过，编译检查通过。本机 Windows build 26200 的 DwmSetWindowAttribute 四项均返回成功；重新打开应用后，实机截图确认标题栏、内容图标及配色一致。保留原生窗口框架。

## 排除自动审批模型

按用户要求从所有日期的统计中排除 `codex-auto-review`。过滤发生在累计区间还原之后，防止被排除的用量转移到下一个正常模型。添加混合模型与纯归档自动审批两项回归，先失败后通过；完整测试现为 34 通过、2 GUI 默认跳过。真实扫描的全部 5 个历史日期均已排除该模型，当日美元汇总不再受其未知报价影响。

## 周／月曲线与额度分组

2026-09-13 本地时间后续请求已完成：日／周／月切换；自然周按实际小时，自然月按本地日期；Token／美元两条曲线，悬停或点击查看数值；保存视图选择。顶部固定显示区间总量，下方内容滚动。

- 最终完整测试开启 `CODEX_NOTE_GUI_TESTS=1`：**63 项全部通过**（含 6 项 Tk 交互／布局测试），compileall 通过。
- 覆盖自然周跨年、闰年月份、日期翻页、未来截断、过去空桶、夏令时 167／169 小时及重复小时、未知价格／缓存写入的费用小计。日、小时聚合共享去重与自动审批排除后的事件。
- 真实日志 189 个文件、5 个日期、34 个有用量的小时。逐日期比较所有模型的日与小时合并行完全相同；周曲线与月曲线的 Token／美元总和均与对应汇总一致。元数据验证摘要保存在 `.local/period-verification.json`，计数会随实时日志增加。
- 此次首次扫描约 3.0 秒，后续增量约 0.1 秒；测试期间仍有日志写入，增量读取 8228 字节，没有重新全量读取。真实 `app.py --report --date` 输出的 days/hours 均限制在所选日期并包含报价。
- 本机日志实证：`codex` 为通用额度，当前仅周窗口；`codex_bengalfox` 为 GPT-5.3-Codex-Spark，包含 300／10080 分钟两窗口。各额度 ID 独立保存最新事件快照，跨文件／同文件互不覆盖；旧 `premium` 空窗口不绘制，无 ID 归为未分类。
- 独立 code-review 发现周期切换未重算 follow_today，会在刷新时将历史日期跳回今天。已修复，并通过模拟当前月→历史日→刷新回归。
- Computer Use 实机检查已确认日视图显示通用周、Spark 5 小时、Spark 周三条额度；周视图每小时曲线、月视图每天曲线及未来留白显示正常。最终版本已重新启动，保留用户窗口大小、位置、置顶和周视图。

## 固定北京时间 UTC+8

2026-09-14 按用户新要求，将默认统计和界面时间从 Windows 本地时区改为固定 UTC+8。统一时钟覆盖启动日期、今日／本周／本月判断、日期选择上限、自动刷新、曲线日历边界、额度快照／重置及刷新时间。总量卡片明确标注「北京时间 · UTC+8」，CLI 日期报告也标明 `timezone: UTC+08:00`。原始日志及美元报价不变。

- 新增北京午夜跨日／周一、月初、额度 ISO／Unix 时间和今日导航／刷新一致性回归；针对原时区行为先确认失败，再实现修正。
- 完整测试启用 GUI：**68 项全部通过**，compileall 通过。最小 390×650 窗口中的时区标签可见。
- 真实日志所有小时桶均带 `+08:00`；各日期模型行与小时合并结果完全一致。9 月 7 日自然周 168 点、9 月 14 日当周及 9 月当月各 14 个已开始时段的 Token／美元之和均与汇总一致（验证时北京时间 13:06）。摘要见 `.local/beijing-verification.json`。
- 真实 CLI `--report --date 2026-09-14` 的日期与小时筛选均按 UTC+8，全部小时带 `+08:00`。
- 已正常关闭并重新启动便签，保留用户最新窗口位置、大小、置顶与日视图。实机确认总量卡片显示「北京时间 · UTC+8」，日志快照及刷新时间显示 13:10，额度重置时间也已转换为北京时间，历史用量重算完成。


## 2026-09-14：模型左右翻页与当前账号

- 模型卡片一次显示一张，显示页码，首尾按钮禁用；刷新重排仍按模型名保留当前选择，空区间与单模型状态通过回归。
- 当前账号每次刷新只读取邮箱与套餐显示元数据，原始 JWT/访问凭据/账号 ID 不进入返回值、设置或报告；使用假凭据测试切换、退出、损坏文件、API Key 模式。
- 保留历史额度未按账号区分的说明；通用额度与 Spark 的两个窗口仍独立显示。
- 主代理运行全部 78 项测试（含 10 项 GUI 布局/交互回归），通过；compileall 通过。原生协作代理只读审查无可操作问题。
- 旧窗口自动控制两次返回 failed to activate captured window，已通过命令只重启本项目便签进程。新版真实窗口确认当前账号行、三类额度及单模型卡；实际点击右箭头到 2 / 2 的 gpt-5.6-sol，再点击左箭头返回 1 / 2 的 gpt-6-astra。


## 2026-09-14：滚动条按需显示

- 内容完全可见时隐藏滚动条，超出可视高度时恢复；隐藏状态下内容填满右侧空间，鼠标滚轮不移动无溢出的内容。
- 10 项现有 GUI 回归与 compileall 通过。另运行实际 Tk 布局检查：390x1000 日视图隐藏、390x650 日视图显示、放大后隐藏、周视图显示、切回日视图隐藏；隐藏时画布与父区域宽度均为 348，显示时画布为 331，确认释放了 17 像素滚动条宽度。
- 界面工具在重启旧窗口时出现用户输入干扰及 foreground window did not report a process id，改为仅重启本项目便签进程加载新版。


## 2026-09-14：窗口位置持续保存

- 先用三轮正常关闭/启动验证已有恢复逻辑可用；未关闭时移动窗口不保存，解释了此前进程重启后回旧位置的原因。
- 改为主窗口移动/缩放后 400ms 合并保存；关闭同步保存，最小化/最大化不覆盖最后正常窗口位置。启动早期与子控件布局事件不写入无效尺寸。
- 新增实际 Tk 回归：移动后无需关闭即可落盘，关闭时立刻保存尚未触发延迟保存的位置，重建窗口准确恢复；最小化关闭保留普通位置。12 项 GUI 测试通过，compileall 通过。
- 已用原生窗口操作 Alt+F4 正常关闭旧版，再从已保存设置启动新版；没有强制终止便签进程。


## 2026-09-14：账号额度与左右分页

- 本机 Codex 0.154.0-alpha.6.2 生成 schema，并参照官方 https://learn.chatgpt.com/docs/app-server；实测 account/read + account/rateLimits/read 响应含 accountId，可与本地登录账号哈希严格核对。
- 当前账号成功获取并持久化：codex 通用每周，codex_bengalfox 的 5 小时及每周窗口。缓存目前 1 个真实账号；其他账号之后登录并成功查询即自动收录，未回填无法归属的历史日志。
- 按账号左右翻页：当前第一，其余按全部已记录窗口中最早重置时间升序（未知时间最后、过期显示待刷新）；普通刷新保持选中账号，登录变化回到第一页。Token 与费用仍汇总本机日志。
- 账号仅存哈希、邮箱、套餐、时间和额度白名单；已直接检查真实缓存字段。客户端临时 stdio 子进程有 15 秒总请求截止，身份变化丢弃，错误不含原始服务响应或凭据。
- 代码审查发现并修复：服务端套餐被本地旧套餐覆盖，以及 daemon worker 关闭时导致查询子进程遗留。加入真实假服务进程的正常完成/超时退出测试，关闭后不再写旧实例缓存。
- 全部 110 项测试通过（含 14 项 GUI 回归、假 stdio 协议与退出路径测试），compileall 通过。测试不请求真实账号，真实接口验证由独立命令完成。
- 新版真实窗口已确认：账号额度标题与 1 / 1 翻页按钮、当前账号邮箱、通用每周及 Spark 两窗口、快照时间、未归属日志折叠入口；按已保存的 958x1000 窗口布局启动。


## 2026-09-14：隐藏 Spark 额度

- 用户要求不再展示 Spark：账号页与未归属日志均隐藏 codex_bengalfox 的 5 小时/每周窗口，账号最早重置排序也排除该 ID。原始快照保留。
- 更新相关既有回归，7 项账号存储/排序与 14 项 GUI 检查通过，compileall 通过；已正常关闭并启动新版，保留窗口设置。

## 2026-09-14：统一账号额度来源与固定钟点刷新

- 根因核实：本地日志的 token_count.rate_limits 没有账号 ID，旧界面将整条日志数据源直接标成「未归属」，未执行账号归属判断。移除该展示路径，账号额度仅取经过身份核对的接口快照；查询失败保留该账号旧快照，没有快照则显示暂无数据。日志 Token／费用统计与 CLI 诊断元数据保留。
- 用量扫描与当前账号查询统一为启动／手动即时刷新，以及北京时间每小时 00、05、10……55 分自动刷新。定时器按当前钟点计算下一次截止，移除原 30 秒扫描和 60 秒账号查询节流。手动刷新不重新计时，延迟回调／休眠恢复只补一次并重新对齐；时钟回拨不提前查询。页脚显示下一次更新时间。
- 新的额度展示检查先在旧实现上复现重复入口；计时回归覆盖普通启动、精确边界、微秒向上取整、跨月午夜、延迟回调与时钟回拨。全部 115 项测试（含 15 项真实 Tk 检查）通过，compileall 通过；修正手动刷新测试的时间夹具后，该项重新运行通过。
- 首轮关闭进程回归有一项父进程启动失败，补充测试错误输出后单项及全套重跑均通过，生产进程退出逻辑未改。正常关闭并启动新版，实机看到 14:46:58 刷新／下次 14:50，账号额度只出现一次，未归属入口消失，窗口设置仍为 390x1000+3441+0。

## 2026-09-14：消费／原生产出等高分页

- 用户明确取消 Issue/MR，只统计 Codex 自带日志。产出使用成功完成的 `item_completed / FileChange` 和 `task_complete`：新增、删除、去重后的文件数及完成轮次，统一按北京时间日／周／月汇总。行数包含代码、文档等文本，是日志中成功修改的累计量，不是 Git 净增；没有原生修改事件的终端写入无法计入。
- 新增 `output.py`：解析后只保留计数、事件标识和路径摘要；按原生事件 ID 去重，覆盖重复扫描、追加、分叉复制、归档移动及文件重写。原读取器的末尾 64 字节改为 SHA256 校验，避免缓存末尾会话片段；Token 计数和报价逻辑不变。
- 新增 `summary_card.py`：消费和产出共用一个布局，启动默认消费。支持左右拖动、左右箭头、标签、键盘方向键及 Shift + 滚轮。刷新／日期切换保留当前页。摘要明细一起固定在顶部，曲线、账号和模型继续在下方滚动。
- 先在旧实现上观察产出测试和分页测试失败，再实现并验证。最终命令：`.venv/Scripts/python.exe -m unittest discover -s tests -v`，环境 `CODEX_NOTE_GUI_TESTS=1`；123 项通过，包括 18 项真实 Tk 布局检查。`compileall` 通过，`--report --date 2026-09-14` 的消费与产出日期过滤通过。完整结果保存在 `.local/output-tests.log`。
- 首轮全量测试曾出现一次 `Tcl_AsyncDelete` 进程退出，随后详细日志运行的 122 项和最终 123 项全量测试均通过；未声称已定位或修复该间歇性测试进程问题。
- 实际 231 份本机日志初扫约 6.17 秒，不变数据复扫约 0.11 秒、读取 0 字节。实机窗口确认两页的标题、总数、三行明细、说明及下方分隔线位置相同；390x650 与 460x800 的 GUI 检查精确比较卡片高度、下方画布位置和高度，无跳动。
- 独立只读代码审查无阻塞问题；审查发现仅产出日期未列入日期下拉框，已补入并用实际选择日期验证。文档和应用内计数说明同步更新。
